# This is a Databricks asset bundle definition for cicdBandle1.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
# giorgip ---> I'm in main
bundle:
  name: oamlops

variables:
  git_url:
    description: Git current url
    default: ""
  git_branch:
    description: Git branch
    default: ""

#sync:
#  include:
#    - my_package/dist/*.whl

include:
   - "bundle_resources/oamlops-pool*.yml"
   - "bundle_resources/oamlops_copy_session.yml"

targets:
  # The 'dev' target, for development purposes. This target is the default.
  dev:
    # We use 'mode: development' to indicate this is a personal development copy:
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default
    # - The 'development' mode is used for Delta Live Tables pipelines
    mode: development
    default: true
    workspace:
      #host: https://adb-958087675976543.3.azuredatabricks.net/
      host: https://adb-2613340384930570.10.azuredatabricks.net/
      #root_path: /Users/${workspace.current_user.userName}/.bundle/${bundle.name}/${bundle.target}
      #artifact_path: /Users/${workspace.current_user.userName}/.bundle/${bundle.name}/dev/${bundle.target}/artifacts
  ## Optionally, there could be a 'staging' target here.
  ## (See Databricks docs on CI/CD at https://docs.databricks.com/dev-tools/bundles/ci-cd.html.)
  qa:
    workspace:
      #host: https://adb-1109565147703855.15.azuredatabricks.net #este es qa
      host: https://adb-958087675976543.3.azuredatabricks.net/ #este es el de produccion que vamos a tener
      #root_path: /Users/${workspace.current_user.userName}/.bundle/${bundle.name}/stg/${bundle.target}
      #artifact_path: /Users/${workspace.current_user.userName}/.bundle/${bundle.name}/stg/${bundle.target}/artifacts
  
  prod:
    # We use 'mode: production' to indicate this is a production deployment.
    # Doing so enables strict verification of the settings below.
    mode: production
    workspace:
      host: https://adb-958087675976543.3.azuredatabricks.net/
      
      # We always use /Users/patricia.giorgi@tomtom.com for all resources to make sure we only have a single copy.
      # If this path results in an error, please make sure you have a recent version of the CLI installed.
    run_as:
      # This runs as patricia.giorgi@tomtom.com in production. We could also use a service principal here,
      # see https://docs.databricks.com/dev-tools/bundles/permissions.html.
      user_name: patricia.giorgi@tomtom.com

